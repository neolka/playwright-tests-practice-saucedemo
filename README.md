# playwright-tests-practice-saucedemo

## Setup

Before running tests, create your own `login-data.json` file 
based on `login-data.example.json`.

Then fill in your real credentials in the login-data.json. 